# CLAUDE.md — Visionary Trades

Guía para Claude Code al trabajar en este proyecto.

## Qué es este proyecto

**Visionary Trades** es un sistema **multi-agente de análisis de flujo de opciones** (options flow). Su propósito es identificar **actividad inusual** en el mercado de opciones —actual e histórica— e interpretarla para dar contexto operativo, incluyendo señales de soporte/resistencia, "muros", flujo direccional vs. cobertura, y noticias relevantes del subyacente.

**Estado actual:** en construcción. La documentación del agente está completa y existe un primer
incremento de la **web interactiva** (`web/`) que lee la option chain desde Massive y muestra
Open Interest, Open Premium y Valor Nocional con pasos de carga en vivo (cubre Tareas 1, 2 y 5).

## Estructura

```
Visionary Trades/
├── CLAUDE.md                        # Este archivo
├── Agente Principal/
│   ├── Proceso Principal.pdf        # Fuente original (Apple Pages/PDF)
│   └── Proceso Principal.md         # Especificación del agente de Opciones (7 tareas)
├── Intrucciones Referencias.md      # Advertencia de liquidez / GEX
├── Intrucciones Referencias.pages   # Fuente original
├── RSS Feed.md                      # Fuentes de noticias a monitorear
├── RSS Feed.pages                   # Fuente original
├── Sub Agentes/                     # (vacío — agentes secundarios por definir)
└── web/                             # App Next.js (lector interactivo) — ver web/SPEC.md
```

## App web (`web/`)

- **Diseño (jul 2026):** tema claro estilo "Options AI Dashboard" importado de Claude Design (proyecto `0017fa5c…`, `Options AI Dashboard.dc.html`). Fuente **Space Grotesk** (next/font). Layout: `HeaderBar` sticky (logo + pills de tickers + búsqueda + precio) → Sentiment/Prediction → Activity/MoneyFlow → **PRO Strike Walls** (oscuro) → TradesFeed → `<details>` "Detalle de sub-agentes" con TODOS los paneles de categorías (esas tablas/promedios alimentan Prediction Pro — no eliminarlas). PredictionCard y la pestaña Accuracy están en estado "próximamente" hasta que estén los 6 sub-agentes.

- **Panel de riesgo + screener de ideas (`/ideas`, jul 2026):** responde "¿qué puedo tradear hoy y cuánto puedo poner?". Escanea **todo el mercado** (no un ticker): `fetchMarketFlow` en `lib/marketsnack.ts` es `fetchFlow` sin `filter[symbol][]` — ambos comparten el cuerpo de paginación `paginate()`. Ruta SSE `app/api/ideas/route.ts` → `classifyFlow` + capa 1 + historial; página `app/ideas/page.tsx`, componentes `RiskProfileCard.tsx` e `IdeasTable.tsx` (dos densidades con el toggle Estudiante/Pro, que se persiste en `visionary.view`).
  - **Cascada de dos capas** en `lib/risk.ts` (PURO, tests en `risk.test.ts`): (1) `passesQualityFilter` descarta lotería (`thetaPctDaily > 5%`, la banda de `thetaScore`), vencidos y `dte < 7`; `isTradeableIdea` añade el umbral `UNUSUAL_TRADE_THRESHOLD`. (2) `sizeFlow` devuelve el **techo de contratos** = `min(por prima, por quema de theta)` y reporta cuál frenó.
  - **Por qué el theta tiene presupuesto propio (`THETA_BUDGET_PCT = 5`) y no comparte el del slider:** la quema se topa en el costo del contrato (una opción larga no puede perder más que su prima), así que `presupuesto/quema ≥ presupuesto/costo` **siempre** — con un solo presupuesto el `min` elegiría la prima el 100% de las veces y la capa de theta sería código muerto. Con presupuesto propio el theta frena de verdad en cuanto la tolerancia pasa del 5%.
  - **El saldo nunca llega al servidor:** el perfil vive en `localStorage` (`visionary.risk.*`) y `sizeFlow` corre en el cliente. La ruta solo devuelve griegos.
  - **Historial** (la evidencia de "el flow inusual después pasa"): reusa `validationScore` de `lib/validation.ts` solo para tickers que ya tienen `data/trades/{TICKER}.json`; los demás salen "sin historial" sin gastar llamada a Massive. Cada escaneo hace `saveTrades`, así que la cobertura crece sola.
  - **Salvaguarda:** `lowLiquidity` → `blocked`, sin número de contratos. El texto es siempre un **techo** ("tu límite es N"), nunca "compra N". Limitación declarada en la UI: el sizing usa el precio de ejecución del flow, no la quote viva (el feed no la da).
- **Watchlist propio + sincronización con broker (jul 2026):** `lib/watchlist.ts` (PURO, tests en `watchlist.test.ts`) + `lib/watchlistLocal.ts` (localStorage) + `lib/outboxStore.ts` (fs en `data/outbox.json`) + `app/api/watchlist/route.ts` + `app/components/WatchlistCard.tsx`. Marcas una idea con ⭐ en `/ideas` y se guarda el **contrato completo** con la **foto del momento**: spot y precio de entrada, y tu sizing (`maxContracts`, `binding`, cuenta y tolerancia de entonces). Re-marcar NO pisa la foto original — es lo que da valor al histórico. Spec: [web/docs/superpowers/specs/2026-07-24-watchlist-broker-sync-design.md](web/docs/superpowers/specs/2026-07-24-watchlist-broker-sync-design.md).
  - **El watchlist vive en el navegador, no en el servidor** (`visionary.watchlist`), por la misma regla que el perfil de riesgo: la entrada guarda tu saldo y tu sizing. Con el archivo único de antes toda la clase compartía un watchlist en un despliegue compartido. `watchlistStore.ts` queda **solo como legado de lectura** para la importación única del viejo `data/watchlist.json`; ya nadie escribe ahí. Contrapartida asumida: no cruza dispositivos.
  - **Los brokers son intercambiables** (`BROKERS` + `BrokerAdapter`): cada uno declara su `kind` (`mcp` | `link` | `copy` | `none`) y su `granularity`. Añadir un broker = añadir una entrada.
  - **Robinhood es el único `mcp`:** MCP oficial (`https://agent.robinhood.com/mcp/trading`, OAuth — no hay que guardar contraseñas), `claude mcp add robinhood-trading --transport http https://agent.robinhood.com/mcp/trading` y luego `/mcp` para autenticar. Su granularidad es **`contracts`**, verificado contra la API real (jul 2026): expone `get_option_watchlist` / `add_option_to_watchlist` sobre una lista de opciones propia (`allowed_object_types: ["option_strategy"]`), separada de la de acciones. Lo que sigue en beta solo-acciones es la **ejecución de órdenes**, no el watchlist — y como **el agente nunca coloca una orden**, no nos limita. Conectarlo da lectura de **todos** los números de cuenta, posiciones e historial de órdenes — no es un permiso menor. Lo único que Robinhood no acepta por API son las **estrategias de varias patas** (spreads): esas siguen solo en su app.
    - **Ningún broker direcciona por símbolo OCC.** Robinhood pide un UUID de instrumento, que solo sale de buscar por subyacente+tipo+strike+vencimiento con `get_option_instruments`. `contractQuery` (en `lib/watchlist.ts`) arma esa búsqueda y **el agente la ejecuta**. El strike va con **4 decimales** (`toFixed(4)`) porque es un filtro exacto sobre cadena: `"20"` no casa con `"20.0000"` y la búsqueda vuelve vacía sin decir por qué. Sin strike o sin vencimiento devuelve `null` en vez de adivinar entre decenas de contratos.
  - **La sincronización la conduce el agente por MCP, no el servidor web.** Al marcar ⭐ con un broker `mcp` se encola en `data/outbox.json` **lo mínimo para identificar el contrato en el broker**, y es `addToOutbox` quien lo recorta según la `granularity`: con `contracts` viajan símbolo/tipo/strike/vencimiento; con `underlying_only`, solo el ticker. Lo que **nunca** cruza el puente navegador→agente son los griegos, tu sizing y tu saldo. La clave de deduplicación (`outboxKey`) es el contrato o el ticker según esa misma granularidad — por eso dos strikes de WULF son dos trabajos en Robinhood y uno solo en Schwab. `pendingOutbox` devuelve los **ítems enteros** (no solo el ticker: el agente necesita strike y vencimiento para resolver el id) y se confirma con `markOutboxSynced`. La cola vieja de solo-tickers se sigue leyendo — los campos del contrato son opcionales a propósito.
  - **Sincronización automática (jul 2026):** `scripts/sync-watchlist.sh` + launchd (`~/Library/LaunchAgents/com.visionarytrades.watchlist-sync.plist`, cada 15 min). Marcas ⭐ y entra solo. Spec: [web/docs/superpowers/specs/2026-07-24-sync-automatico-robinhood-design.md](web/docs/superpowers/specs/2026-07-24-sync-automatico-robinhood-design.md).
    - **Por qué launchd y no `CronCreate`:** los trabajos de `CronCreate` son de sesión — mueren al cerrar Claude, caducan a los 7 días y solo disparan con el REPL ocioso. launchd sobrevive a cerrar sesión y a reiniciar.
    - **Por qué NO puede hacerlo el servidor web:** Robinhood **no tiene OAuth público para acciones/opciones** (solo cripto, y sus ToS prohíben el acceso automatizado). El único token lo emitió Robinhood **a Claude**, vive en el keychain, caduca fuera de nuestro control y es **de un solo usuario** — con él en el servidor, el ⭐ de cualquier estudiante escribiría en la cuenta de Víctor. Plaid/SnapTrade hacen OAuth contra Robinhood pero exponen posiciones y órdenes, **no el watchlist**. Consecuencia asumida: **esto es solo para la máquina de Víctor**; los estudiantes se quedan en enlace/copiar.
    - **El shell hace lo determinista y el modelo solo lo que necesita un modelo.** El guion lee la cola, topa a 10, confirma y registra; `claude -p` solo resuelve el contrato → UUID de instrumento, con una lista blanca de **exactamente 3 herramientas** (`get_option_instruments`, `get_option_watchlist`, `add_option_to_watchlist`). Sin Bash y sin `place_option_order`: el proceso es **incapaz** de colocar una orden. **Con la cola vacía no se invoca al modelo** — coste cero en los pases en vacío.
    - **Idempotente:** consulta `get_option_watchlist` antes de añadir, así que si el `POST synced` falla tras haber añadido, el pase siguiente solo marca. **Solo añade, nunca borra** del broker. Bitácora en `data/sync-log.jsonl`.
    - **Estado terminal sin reintentos:** `markOutboxFailed` aparca lo irresoluble (`failedAt`/`failReason`) y `pendingOutbox` lo excluye. Sin contador de intentos a propósito: cuando `contractQuery` da `null` o el broker no encuentra el contrato, ya se sabe que no se resolverá nunca. Los fallos transitorios no marcan nada y se reintentan solos.
    - **Encolar sin strike o sin vencimiento devuelve 400** con granularidad `contracts`. Aceptarlo solo aplazaba el fallo hasta el drenador, donde ya no hay a quién avisar.
    - **Ojo con `removeFromOutbox`:** `outboxKey` vale el símbolo OCC en las filas nuevas y el **ticker** en las viejas, así que filtrar solo por `symbol` dejaba las filas legado **imborrables** (`"WULF270115C00020000" !== "WULF"`) — SPXW y SPY siguieron encoladas tras desmarcarlas. El cliente manda ahora símbolo **y** ticker; la segunda condición exige `!i.symbol` para no arrastrar los strikes hermanos.
  - **Los `link` y los `copy` se decidieron probando las URLs, no suponiendo.** Robinhood/Schwab/Fidelity/Tastytrade tienen página por símbolo. **Webull e IBKR son `copy` a propósito:** Webull exige el prefijo de bolsa (`nasdaq-wulf` responde, `nyse-wulf` da 404) y el feed no trae la bolsa; la página de IBKR responde 200 pero es genérica. Mandar a un estudiante a un 404 es peor que darle el ticker para pegar — `quoteLink` devuelve `null` y la UI enseña "copiar".
  - **Ojo con el estado en `page.tsx`:** el watchlist se lee de `wlRef` (ref siempre fresca), no del estado de React. Marcar una estrella mientras la carga inicial seguía en vuelo escribía sobre un array vacío y borraba lo ya guardado — pasó de verdad con el WULF migrado.
- **Wheel Strategy (`/wheel`, jul 2026):** screener de cash-secured puts que responde "qué put vendo hoy y cuánto efectivo inmoviliza". Universo curado de 40 tickers (`lib/wheelUniverse.ts`), 3 presets (`WHEEL_PRESETS` en `lib/wheel.ts`), score compuesto 0-100 que reusa `findLevels` (soportes), proxy de IV Rank y estimador de earnings (`lib/earnings.ts`). Criterio PURO y testeado en `lib/{blackScholes,wheel,earnings,wheelAfford}.ts`; ruta SSE `app/api/wheel/route.ts` solo orquesta I/O. El saldo vive en `localStorage` y la asequibilidad se calcula en el cliente (`sortByAffordThenScore`), nunca en el servidor. **Ojo:** la banda de IV Rank en `wheel.ts` va INVERTIDA respecto a `ivcontext.ts` — la Wheel vende volatilidad, el resto del agente la compra. Spec: [docs/superpowers/specs/2026-07-24-wheel-strategy-design.md](web/docs/superpowers/specs/2026-07-24-wheel-strategy-design.md).
- **Venta de Primas (`/venta-de-primas`, ago 2026, pedido explícito de Carlos con un prompt muy detallado):** screener de **spreads de crédito de riesgo definido** (21-45 DTE) sobre el mismo universo curado de `lib/wheelUniverse.ts`. "El agente calcula y muestra; vos decidís y ejecutás" — no es orden ni consejo, y la interfaz lo dice. Matemática PURA en `lib/creditSpreads.ts` (tests literales de las 8 trampas del prompt en `creditSpreads.test.ts`); I/O en `lib/massive.ts` (`fetchCreditSpreadChain`) y `app/api/venta-de-primas/route.ts` (SSE, clon de la estructura de Wheel); UI en `app/venta-de-primas/page.tsx` + `VentaPrimasRiskCard.tsx`/`PopFilterTabs.tsx`/`VentaPrimasTable.tsx`.
  - **Las trampas que el prompt pedía evitar, una por una:**
    1. **POP en el BREAKEVEN, no en el strike corto.** `popAtBreakeven` mide "gano dinero" (precio más allá del breakeven), no "gano el máximo" (precio más allá del strike corto). Nota de honestidad dejada en el código: el prompt describe la diferencia como "gano dinero" vs. "gano el máximo" y dice que medir en el strike corto da un número "más alto" — pero como "ganar el máximo" es un SUBCONJUNTO de "ganar dinero", matemáticamente POP(breakeven) ≥ POP(strike corto) SIEMPRE, la relación inversa a la que describe la prosa del prompt. Se implementó y testeó la dirección matemáticamente correcta (consistente con la propia lógica "gano dinero" ⊇ "gano el máximo" que da el prompt), documentando la discrepancia en vez de escribir un test que exigiera algo matemáticamente imposible.
    2. **Dos IVs, una por pata corta — nunca prestada de la otra** (el skew put/call es real). `iv` se calcula por contrato individual, nunca como un ATM compartido.
    3. **El valor esperado usa volatilidad REALIZADA (22 cierres), nunca la implícita.** `expectedValueSpread`: integración numérica (400 puntos, ±4σ, regla del punto medio) sobre una lognormal SIN DERIVA (r=0, mismo convenio que `expectedMove.ts`). Test de regresión que Carlos pidió no borrar: con la MISMA vol que "puso el precio" (`bsPrice(...,r=0)`, consistente con el driftless de la integración), el VE da ~0.
    4. **Crédito = vender al BID, comprar al ASK** (`conservativeCredit`) — el llenado malo a propósito.
    5. **Ancho = MEDIANA de los huecos de la cadena** (`medianStrikeWidth`), no un valor fijo ni la media (un hueco suelto no la desvía).
    6. **Colateral = (ancho×100) − crédito**, nunca más.
  - **Limitación real re-verificada EN VIVO (no la nota vieja de jul 2026 sobre Wheel que decía lo contrario):** `/v3/snapshot/options/{ticker}` en el plan actual de Massive **NO trae `last_quote` (bid/ask) ni `last_trade` para NINGÚN contrato** — probado en vivo sobre IWM/AAPL/SPY, 0 de 250 resultados con bid en los tres. Sí trae `day.close`/`day.vwap` (~209/250 contratos) y, novedad frente a la nota vieja de Wheel, `implied_volatility` casi siempre (250/250 en la prueba). Consecuencia: el crédito "vender al bid, comprar al ask" se degrada a una ESTIMACIÓN conservadora desde el último precio cuando no hay bid/ask real — `resolveLegQuote` (lib/creditSpreads.ts) ensancha `±10%` sobre `last_trade → day.close → day.vwap` (mismo `HAIRCUT.ultimo` que ya usa `lib/wheel.ts` para el mismo problema). Cada candidato lleva `creditSource: "real" | "estimado"` y la tabla marca la fila — nunca se presenta como precio real cuando no lo es. Tampoco hay `underlying_asset.price` en este endpoint: el spot sale del último cierre de `cachedDailyBars` (las mismas barras que ya se piden para la volatilidad realizada), no de la cadena de opciones.
  - **Earnings — fecha de ANUNCIO, no de filing, y la regla de horario del borde.** Reusa `fetchFilingDates`/`estimateNextEarnings` de `lib/earnings.ts` sin tocar ese archivo (es de producción de Wheel). Tipo nuevo `EarningsTiming = "before_open" | "after_close" | "unknown"` y `earningsWithinExpiration` (PURA, en `creditSpreads.ts`): cualquier fecha estrictamente antes del vencimiento cuenta como "dentro"; el MISMO día decide la hora (`after_close` → fuera; el resto → dentro); sin hora conocida, "dentro" (regla conservadora: equivocarse por prudente cuesta una oportunidad, equivocarse al revés cuesta el colateral). **Re-verificado en vivo (ago 2026, no asumido de la nota vieja):** `/benzinga/v1/earnings` → 403 "no entitled"; `/v1/reference/earnings`, `/vX/reference/earnings` y `/v3/reference/tickers/{t}/events` → 404, los tres en el plan actual. Ninguna fuente conectada da la HORA del anuncio hoy — se usa `"unknown"` siempre en producción (declarado en la UI, no oculto), que por la regla conservadora cae en "dentro".
  - **Guarda "REPORTE DENTRO":** insignia roja junto al ticker + recuento visible encima de la tabla (`meta.earningsWithinCount`). Nunca se filtran en silencio — la prima alta antes de un reporte es alta justo por el riesgo del reporte.
  - **Veredicto de mercado agregado:** `marketVerdict` (lib/creditSpreads.ts) compara la volatilidad realizada promedio del universo escaneado contra la implícita promedio de las patas cortas generadas; si la realizada gana, banner rojo fijo encima de la tabla ("hoy la prima está barata en el mercado") que NO se oculta aunque haya candidatos con buena pinta.
  - **PRIMA (CARA/NORMAL/BARATA):** `classifyPremium` reusa `rankWithin` de `lib/ivcontext.ts` — percentil de la IV de la pata corta dentro de la serie de volatilidad realizada histórica del propio ticker (22 días, ventana móvil sobre ~400 días de barras). Cortes 30/70 documentados como elección propia (el prompt no fija banda oficial para esto, a diferencia del IV Rank del scorecard).
  - **Sesgo direccional — heurística declarada como burda:** `biasFromCloses` (creditSpreads.ts) solo mira si el precio de hoy está por encima o por debajo del cierre de hace 20 sesiones; decide put credit spread (alcista) vs. call credit spread (bajista). A propósito más cruda que la ficha individual del ticker (niveles/GEX/flujo real) — declarado en la UI, remite a esa ficha antes de operar.
  - **Perfil de riesgo propio** (`CreditSpreadRiskProfile` en `lib/creditSpreads.ts`, `localStorage.visionary.ventaPrimas.*`, nunca al servidor): cuenta + slider de riesgo 1-50% → capital máximo por trade; máxima quema de theta = 5% de ese capital (regla propia de Carlos, solo informativa en el panel — no hay griegos reales de theta por contrato en este plan de datos, así que no filtra candidatos, a diferencia del theta de Ideas en `lib/risk.ts`). CABEN (`contractsThatFit`) se calcula en el CLIENTE, mismo patrón que `wheelAfford.ts`.
  - **Orden por VE/$, nunca por crédito absoluto** (regla explícita del prompt): un crédito grande sobre un colateral enorme es peor negocio que uno pequeño sobre poco colateral.
  - **POP nunca redondeado hacia arriba:** la UI trunca (no redondea) al mostrar el porcentaje.
  - **Universo:** reusa `WHEEL_UNIVERSE` (40 tickers) tal cual — el criterio de Wheel incluye "sería aceptable poseerla", que no aplica a spreads de riesgo definido (nunca asignan el subyacente completo), pero sigue siendo un universo líquido y opcionable razonable, y evita reinventar uno nuevo.
- **Noticias (Tarea 7):** `lib/news.ts` + `app/api/news/route.ts` + `app/components/NewsCard.tsx`. **Dos capas:** macro (los RSS de [RSS Feed.md](RSS%20Feed.md), cache 15 min) + empresa (`/v2/reference/news?ticker=` de Massive, que trae `insights[].sentiment` por ticker, cache 5 min). Los titulares macro que mencionan a la empresa se promueven a la capa de empresa. **Bandera de contradicción** (`contradictionFlag`) confronta la dirección del flujo contra el sesgo de noticias — **no toca los 100 pts del scorecard**. Tests en `lib/news.test.ts`.
- **Stack:** Next.js 15 (App Router, TS). Correr con el dev server `visionary-web` (`.claude/launch.json` a nivel Desktop) o `npm run dev` en `web/` (puerto 3000).
- **Proveedor de datos:** Massive (`api.massive.com`, rebrand de Polygon.io). Endpoints: option chain `GET /v3/snapshot/options/{ticker}` (paginado), detalles empresa `GET /v3/reference/tickers/{ticker}`, snapshot acción `GET /v2/snapshot/locale/us/markets/stocks/tickers/{ticker}`.
- **Vista Estudiante vs Pro (jul 2026):** toggle en `page.tsx` (`view`, default `estudiante`). La vista **Estudiante** es limpia para novatos y NO toca `lib/` (cálculos) — solo re-empaqueta lo ya calculado: `VeredictoCard` (frase llana desde `prediction.summary` + dirección + confianza; muestra "no fiable/no operar" si `prediction.caveat`), selector de horizonte llano (Esta semana/2 semanas/1 mes → 10/20/30), `SimpleChart`, `EscenariosCard` (los **3 targets** bajista/base/alcista con % y prob), `ContextoLinea` (noticias en 1 línea + bandera de contradicción), `NivelesSimples` (lista mínima con `probTouch` por nivel), y `MemoriaCard`. Sin jerga (GEX/gamma/notional viven solo en Pro). La vista **Pro** es el dashboard completo original sin cambios. Spec: [docs/superpowers/specs/2026-07-24-vista-simple-estudiantes-design.md](docs/superpowers/specs/2026-07-24-vista-simple-estudiantes-design.md).
  - **`SimpleChart`:** velas + los **3 escenarios** como líneas que SE MUEVEN (helper `wigglePath`: ruta `predictionPath` + oscilación ∝ σ con un sobre `sin(πf)` que vale 0 al inicio y al target, así ancla ambos extremos y "baila" dentro del cono; `seed` distinto por escenario, determinista). Se dibujan una vez (`stroke-dashoffset`) sobre `PriceChart`. Ruido reducido: solo los **2 soportes + 2 resistencias más cercanos** con fuerza ≥25. El **target base = nivel imán del GEX** (mayor probabilidad×premium) recortado al cono 2σ; por eso en flujo lateral la línea sale casi plana.
  - **Memoria del agente (auto-evaluación):** `lib/predictionStore.ts` (fs en `data/predictions/{TICKER}.json`, dedupe por fecha ET, se acumula hacia adelante) guarda una foto diaria con los 3 targets; `reviewPredictions` (PURA, tests en `predictionStore.test.ts`) la compara días después contra las barras reales: error del base, si tocó el nivel, acierto de dirección, **sesgo** (error medio firmado = si sistemáticamente apunta alto/bajo) y qué escenario acertó. Ruta `app/api/prediction/route.ts` (POST guarda al cargar desde `page.tsx`; GET revisa). Panel `app/components/MemoriaCard.tsx`. Al principio dirá "aún no hay predicciones vencidas" hasta que pase el horizonte.
  - **Auto-corrección por memoria (lazo de control):** `predictPro` acepta `calibration: { biasPct, samples }` y `calibrationShiftPct` (tests en `prediction.test.ts`) corrige el **target base** según el sesgo histórico. Amortiguado y acotado (`CALIBRATION = { minSamples: 5, gain: 0.6, capPct: 3 }`): solo con ≥5 vencidas, corrige el 60% del sesgo, tope ±3% del spot, y se recorta al cono 2σ. El imán crudo sigue anclando la búsqueda de bull/bear (no se arrastra el sesgo a los extremos). Converge: al mejorar, el sesgo baja y la corrección se apaga sola. `page.tsx` lee el sesgo (GET `/api/prediction`) ANTES de fijar el target y **guarda el target ya calibrado** (espera `calibReady`). Chip `🧠 ajustado ±X%` en `VeredictoCard` + nota en el resumen cuando aplica.
- **Panel de empresa:** antes de la tabla se muestra logo + info + stats (Stock Price, market cap, volumen, rango del día, cierre previo, empleados). El logo se sirve por proxy propio `GET /api/logo?ticker=` (la key nunca llega al cliente). La tabla tiene fila TOTAL con la sumatoria (incl. Notional).
- **Estructura / Acumulación y Rapidez (categoría 4 del scorecard):** `lib/structure.ts` → `structureScore` (nocional promedio por strike, dominio direccional calls/puts en los top-5 strikes, % volumen > OI; ver [SCOREDCARD/Acumulacion-Rapidez.md](SCOREDCARD/Acumulacion-Rapidez.md)). Panel en `app/components/StructureCard.tsx`. **Usa la cadena de opciones de Massive**, no el flujo de MarketSnack. Historial de 45 días: `lib/chainStore.ts` guarda una foto por día de mercado en `data/chain/{TICKER}.json` (dedupe por fecha ET) — se acumula hacia adelante porque Massive no expone OI histórico.
- **Confirmación de Precio / Validación de Flows (categoría 6 del scorecard):** `lib/validation.ts` → `validationScore` (backtest: para cada flow guardado mide MFE/MAE y cuántas sesiones tardó el movimiento a favor y en contra; ver [SCOREDCARD/Validacion-Flows.md](SCOREDCARD/Validacion-Flows.md)). Ruta `app/api/validation/route.ts`, panel `app/components/ValidationCard.tsx`. **El PDF no trae tabla de puntos** — las bandas de `validationPoints`/`speedPoints` son una propuesta y están aisladas para cambiarlas de un sitio. Umbral de movimiento **adaptativo** (rango diario típico × 1.5, piso 2%) porque un 2% fijo satura en tickers volátiles. Corre sobre `data/trades/{TICKER}.json`, que se acumula hacia adelante; avisa si no llega a los 60 días que pide el documento.
- **Soportes y resistencias:** `lib/levels.ts` → `findLevels` (puro, tests en `levels.test.ts`). Cruza **dos fuentes independientes**: (1) precio — `findPivots` (swing highs/lows con ventana k) + `clusterPivots` (agrupa por tolerancia %, así $299 y $301 son un solo nivel) con peso por `recencyFactor`; (2) opciones — según la tabla del Proceso Principal **vender calls = resistencia, vender puts = soporte**, así que solo cuentan calls para resistencias y puts para soportes, y solo la ejecución al **bid** (venta) suma como muro. Fuerza 0-100 = toques·frescura + OI + premium de flujo + |GEX| + **bonus de confluencia** cuando coinciden precio y opciones. Los strikes sin rebote previo deben superar el **percentil 70 de OI** para entrar, si no la lista se llena de ruido. Marca niveles `flipped` (era techo y ahora hace de suelo). Panel `app/components/LevelsCard.tsx` y líneas punteadas en `ProWallsCard` para los de fuerza ≥35.
- **Prediction Pro (cierre del sistema):** `lib/prediction.ts` → `predictPro` (puro, tests en `prediction.test.ts`). Junta los 6 sub-agentes + mapa GEX + σ en **tres escenarios**: `base` = nivel imán del heatmap, `bull`/`bear` = el nivel relevante arriba/abajo **excluyendo el base**, con fallback a las bandas de 1σ. Se fuerza el orden estricto **bear < base < bull** y todo se recorta al cono de 2σ. Cada escenario trae precio, %, probabilidad de toque y el porqué. `weightedScore` da el sentiment 0-100 con los pesos del scorecard; `confidenceOf` mezcla nitidez del imán + cobertura de sub-agentes + hit rate del sub-agente 6. Genera un **resumen en lenguaje llano** y avisos (baja liquidez → NO FIABLE; faltan categorías → confianza recortada). Panel `app/components/PredictionCard.tsx` con selector de horizonte **10/20/30 días** y los **top 3 flows** por premium. El horizonte vive en `page.tsx` y lo comparten PredictionCard y ProWallsCard.
- **Gráficas propias en SVG (jul 2026):** `SimpleChart` y `ProWallsCard` YA NO usan TradingView. Motor propio: `lib/chartGeometry.ts` (PURO, tests en `chartGeometry.test.ts`) + `app/components/chart/PriceChart.tsx` (dibujo, un solo `<svg>`) + `ChartCrosshair.tsx` (crosshair + tooltip, sin zoom ni paneo). Antes había **dos sistemas de coordenadas** peleándose —el canvas de la librería y un overlay HTML que cazaba píxeles con `priceToCoordinate`/`priceScale().width()`— y de ahí venían el futuro comprimido y los targets encimados. Tres funciones: `smartDomain` (encuadre que cubre velas + 1σ + targets con peso, y estira al 2σ solo mientras las velas conserven el 45% del alto), `buildScales` (reparto **60% histórico / 40% futuro** con `xNow` explícito; recorta el histórico a las velas que caben con ancho ≥3px), `packLabels` (anti-colisión de los chips en dos barridas; guarda `yAnchor` en el precio real para la guía punteada). La clave del layout es el **gutter derecho de 132px** reservado a los chips — el eje de precio se rotula dentro del área de dibujo. Alturas responsivas por CSS (`clamp`), no números fijos. Bonus: al ser SVG, los screenshots del preview ya no salen negros. Spec: [docs/superpowers/specs/2026-07-24-graficas-propias-svg-design.md](docs/superpowers/specs/2026-07-24-graficas-propias-svg-design.md). **`ChartPanel` y `FlowPriceChart` siguen con `lightweight-charts`**, así que la dependencia se queda.
- **Movimiento esperado y probabilidad por nivel:** `lib/expectedMove.ts` (puro, tests en `expectedMove.test.ts`) — `expectedMove` (σ = S·IV·√(T/365), bandas 1σ/2σ lognormales), `conePoints` (cono que se abre en √t), `probAbove`/`probInBand`/`probTouch` (lognormal sin deriva; el toque usa principio de reflexión ≈2× la de cierre) y `levelProbabilities` (mezcla normalizada de probabilidad de toque × concentración de dinero del GEX → el % de cada banda). `predictionPath` traza la ruta al nodo imán y la **recorta al cono de 2σ**. `ProWallsCard.tsx` ya no dibuja burbujas: pinta **bandas de heatmap con su probabilidad** + cono 1σ/2σ + ruta esperada, todo dentro del SVG propio (ver la viñeta de gráficas propias).
- **GEX Heatmap por strike × vencimiento:** `lib/gexHeatmap.ts` → `gexHeatmap` (celda = GEX neto de un strike en una expiración; gamma Black-Scholes anclada a la gamma real de MarketSnack donde ese strike/vencimiento operó). Panel `app/components/GexHeatmapCard.tsx`: filas = strikes (±18 alrededor del spot), columnas = 8 vencimientos más cercanos, verde γ+ / morado γ−, columna Total por strike, fila del spot resaltada y la malla se auto-centra en el precio actual.
- **Contexto IV (categoría 5 del scorecard):** `lib/ivcontext.ts` → `ivContextScore` (2 parámetros: IV actual con pico en 40-60%, e IV Rank con pico en 16-30%; ver [SCOREDCARD/Contexto-IV.md](SCOREDCARD/Contexto-IV.md)). Panel en `app/components/IvContextCard.tsx`. **La IV sale de MarketSnack** (`implied_volatility`, en decimal → ×100), ponderada por premium. El **IV Rank** usa un proxy de volatilidad realizada del subyacente hasta que `lib/ivStore.ts` acumule 60 fotos diarias en `data/iv/{TICKER}.json` (ventana 365 días), momento en que el rank real lo reemplaza solo. Deriva también el **skew del frente** (evento inminente si > +10 pts) y el régimen (dormida/compresión/normal/expansión/inflada). Tests en `lib/ivcontext.test.ts`.
- **Inusualidad (categoría 3 del scorecard):** `lib/flow.ts` → `unusualityScore` (6 parámetros de griegos: tamaño, delta, theta%, gamma, single/multileg, vencimiento; ver [SCOREDCARD/Inusualidad.md](SCOREDCARD/Inusualidad.md)). Panel en `app/components/UnusualityCard.tsx`. Usa la misma ventana de 30 días que Convicción.
- **Convicción (categoría 2 del scorecard):** `lib/flow.ts` → `convictionScore` (spread, dominancia ask/bid, fuerza de ejecución; ver [SCOREDCARD/Conviccion.md](SCOREDCARD/Conviccion.md)). Panel en `app/components/ConvictionCard.tsx`.
- **Time & Sales (sub-agente Agresividad):** vista `app/flow/` + `app/api/flow/route.ts` (SSE). Fuente de datos = **MarketSnack** (producto propio del usuario), endpoint interno `GET app.marketsnack.com/api/flow_feed?filter[scope]=all&filter[symbol][]=<T>&period=5d` (paginado por `next_page_token`), auth por cookie de sesión (ver viñeta de abajo — ya no vive solo en `.env.local`). Da bid/ask + `side` (ask/bid/mid) + greeks + premium — resuelve la agresividad que Massive no autoriza. Lógica pura en `lib/{marketsnack,flow,occ}.ts` (parseo OCC, clasificación, flags de "interesante": ≥$1M, ≥$100K & |Δ|>.60, above ask/below bid, repetidas 5min, multileg). Tests en `lib/{occ,flow}.test.ts`.
- **Cookie de MarketSnack — actualización sin reiniciar (ago 2026):** antes había que pegarla a mano en `.env.local` y **reiniciar** `next dev` (Next solo lee `.env*` al arrancar). Ahora `lib/marketsnackCookie.ts` la guarda en `data/marketsnack-cookie.json` (gitignored) y se **lee en cada petición** — `.env.local` sigue funcionando como respaldo si ese archivo no existe todavía. Página [/ajustes](web/app/ajustes/page.tsx) + ruta `app/api/marketsnack-cookie/route.ts` (GET estado / POST guardar): el POST **prueba la cookie contra MarketSnack antes de guardarla** (`testMarketSnackCookie` en `lib/marketsnack.ts`, pega a `/api/assets/SPY`) — si no sirve, se rechaza y la cookie que ya funcionaba no se toca. Normaliza lo pegado (quita comillas/saltos de línea/prefijo `"Cookie:"`) y solo muestra una huella parcial, nunca la cookie completa, al cliente.
  - **Mantener viva la sesión:** `web/scripts/marketsnack-keepalive/` (`keepalive.mjs` standalone + `Instalar/Desinstalar-KeepAlive.ps1` vía `schtasks`, cada 25 min) toca `/api/assets/SPY` para que no caduque por inactividad. **Sin garantía:** si MarketSnack usa caducidad absoluta (no por inactividad) esto no ayuda — solo se sabe observando el log unos días.
  - **Extractor automático desde el navegador — NO FUNCIONA todavía:** `web/scripts/marketsnack-cookie/` (PowerShell hace DPAPI-unprotect de la clave maestra de Chrome/Edge; Node con `node:sqlite` + AES-256-GCM descifra las cookies, sin dependencias nuevas). Probado en real (2026-08-02): el DPAPI y el descifrado funcionan, pero **Chrome/Edge bloquean el archivo de cookies a nivel de SO** incluso con `FileShare.ReadWrite+Delete` — el candado lo pone el navegador al abrirlo, no algo que se evada desde el lado lector. No es App-Bound Encryption (v20) — la clave maestra sigue con prefijo `DPAPI`. El script detecta el bloqueo y lo dice con ese mensaje exacto; el único respaldo real hoy es pegar la cookie a mano en `/ajustes`.
- **Mapa de nodos GEX & predicción (PRO):** `lib/gex.ts` → `gexAnalysis` (puro, tests en `lib/gex.test.ts`). Massive no da gamma/IV, así que la IV se estima de la volatilidad realizada de las barras diarias, la gamma con Black-Scholes por contrato, y se **ancla** a la gamma real de MarketSnack donde el strike operó. GEX por strike = `gamma × OI × 100 × spot² × 0.01` (+call/−put); **concentración** = 0.6·|GEX| + 0.4·premium de trades reales en ese strike. Deriva **nodo principal/imán** (precio objetivo), **zona de inversión gamma** (flip), **régimen** (γ+ revierte / γ− amplifica) y **confianza** (nitidez + scores de Convicción/Estructura). `ProWallsCard.tsx` dibuja las velas y los muros con `PriceChart` (SVG propio) y realimenta la predicción a `PredictionCard.tsx`. Hereda la salvaguarda de liquidez (si es ilíquida → "no fiable"). El GEX se calcula una vez en `page.tsx`.
- **Burbujas de repetición:** `app/components/RepeatBadge.tsx` (`🔁 ×N`) marca los trades repetitivos (`flags.repeated` — mismo strike ≥3× en 5 min) en TradesFeed, ConvictionTransactions y UnusualityCard.
- **Gráfica Top 5 por Notional:** `app/ChartPanel.tsx` usa TradingView **Lightweight Charts** (`lightweight-charts`) para dibujar el candlestick del subyacente (barras de `GET /api/history` → `/v2/aggs/...`) con una price line por cada uno de los 5 contratos de mayor Notional, más leyenda (contrato, vencimiento, OI, Open Premium, Notional). Nota: el canvas de la gráfica sale negro en screenshots del preview (limitación de captura), pero renderiza bien en el navegador real; verificar por análisis de píxeles si hace falta.
- **API key:** en `web/.env.local` como `MASSIVE_API_KEY` (server-only, gitignored). **Nunca** exponerla al cliente ni publicarla.
- **Progreso en vivo:** el route handler `app/api/chain/route.ts` transmite los pasos por SSE; el frontend usa `EventSource`.
- **Cálculos:** en `lib/compute.ts` (funciones puras, con tests en `lib/compute.test.ts`, `npm test`).
- **Limitación del plan actual:** Massive **sí** devuelve `last_quote` (bid/ask) en el Option Chain Snapshot (verificado jul 2026), pero **no** `greeks` ni `implied_volatility`. Open Premium sigue usando `last_trade.price ?? day.close ?? day.vwap` como proxy; el delta de la Wheel se calcula por Black-Scholes (`lib/blackScholes.ts`).
- **Prueba de Fuego — day-trading en vivo (`/prueba-de-fuego`):** pestañas TSLA/SPX con sugerencia de contrato EN VIVO (`lib/dayTrade.ts` → `suggestDayTradeContract`, ruta SSE `app/api/daytrade/route.ts`, componente `TickerLiveTab.tsx`). Una sola sugerencia **fija por día de mercado** (se guarda en `localStorage`, ver `lib/dayTradePosition{,Local}.ts`) con P&L en vivo contra el precio actual del contrato. Dirección/target salen de caminar los **strikes vecinos reales** (net premium call vs put, `lib/neighborContracts.ts` → `neighborContractsEntrySignal`, `NEIGHBOR_WALK_COUNT = 4`) desde el strike más cercano al spot hasta el primer volteo; sin desequilibrio ahí, cae al GEX como señal débil (`role: "gex_only"`, solo SPX — TSLA es solo MarketSnack). También existe `lib/backtest.ts` + `app/api/backtest/route.ts`: backtest DIARIO (no intradía) de TSLA con el mismo `gexAnalysis`, distinto de todo lo de abajo.
  - **"SPX vecinos" (ago 2026, pedido explícito de Carlos):** pestaña separada con un motor de señal **por magnitud de dinero**, no por dominancia local — nació de corregir a mano un backtest real. Ejemplo real (SPX, 31-jul-2026, 09:45 ET, spot $7456.64): el walk de siempre veía "CALL domina en el centro ($7455, apenas +$60,824)" y operaba ahí (perdió -10.2%), mientras un strike más allá ($7460) había **+$471,794 en puts** — casi 8x más grande — que el walk nunca comparaba. `wallEntrySignal` (`lib/neighborContracts.ts`) busca la pared con mayor `|callNet − putNet|` en **todo** el vecindario (6 strikes por lado — `WALL_NEIGHBOR_COUNT`, no 4), esa pared fija la dirección, y devuelve **dos targets**: el primer strike que confirma esa dirección caminando desde el spot, y el siguiente más allá si lo hay — más `resistance`/`support` (la pared más grande de cada lado del spot). Alimenta `suggestWallContract` (`lib/dayTrade.ts`, **sin** respaldo de GEX a propósito) → ruta `app/api/daytrade-wall/route.ts` → componente propio `SpxVecinosTab.tsx` (NO reusa `TickerLiveTab`: acá no se fija una posición del día, es un feed que se **refresca solo cada 5 min** o al tocar "Nueva sugerencia"). Vista deliberadamente chica: precio, resistencia, soporte, la decisión call/put, y los dos targets con su net premium — sin la tabla de niveles/GEX de `SpxLevelsCard`. Aplica también al botón **TSLA** (mismo componente `SpxVecinosTab.tsx`, ticker distinto). **Update ago-2026 (pedido explícito de Carlos):** se quitó la regla de no operar los primeros 15 minutos tras la apertura (`isWithinOpeningMinutes` eliminada de `lib/marketHours.ts`) — ahora opera desde que abre el mercado. También se eliminó la pestaña "SPX amiga" (`SpxAmigaTab.tsx`, `lib/spxAmiga.ts`, `app/api/spx-amiga/`) a pedido de Carlos.
  - **"Contratos vecinos 2.0" (ago 2026, pedido explícito de Carlos):** a diferencia de "SPX vecinos" (que decide la DIRECCIÓN pesando la magnitud del vecindario), acá la dirección la fija el **imán del GEX** — `zeroDteGex` (`lib/zerodte.ts`, griegos REALES de tastytrade, el mismo motor de Agente ODTE) sobre la cadena 0DTE de SPX del día, no la estimación Black-Scholes de `lib/gex.ts`. `lib/magnetWall.ts` → `magnetWallSignal` (PURO, tests en `magnetWall.test.ts`) combina esa dirección con el net premium real de MarketSnack en un vecindario de **10 strikes por lado** (`MAGNET_WALL_NEIGHBOR_COUNT`), clasificando cada strike con las 4 reglas exactas que dio Carlos: `netPremium(call)>0` = compra agresiva → alcista, `netPremium(put)>0` = compra agresiva → bajista, `netPremium(call)<0` = venta → resistencia, `netPremium(put)<0` = venta → soporte/hedge. Esas 4 señales se funden en un único `netBias` por strike (compra de calls + venta de puts suman a favor de subir; compra de puts + venta de calls suman a favor de bajar) — más simple de caminar que arrastrar las 4 categorías por separado. Camina desde el spot hacia el imán juntando hasta 3 strikes confirmados + el imán mismo como target final (**4 targets "hacia el imán"**, `MAX_STEP_TARGETS`, pedido explícito de Carlos ago-2026); en paralelo camina el lado CONTRARIO buscando hasta **4 targets de "ruptura"** (`MAX_BREAKOUT_TARGETS`) (si el precio rompe ahí, invalida la tesis del imán) — apartado separado en la UI, con una probabilidad más baja a propósito (`COUNTER_DISCOUNT` en `magnetWall.ts`: van contra el régimen que ancla el GEX, no solo contra la distancia). La probabilidad de cada target mezcla `probTouch` (estadística: distancia + IV ATM + horas al cierre, `lib/expectedMove.ts`) con la agresividad real del net premium en ese strike (normalizada dentro del propio vecindario) — nunca se muestra 0% ni 100%. **Libertad explícita de decir "no operar":** si el imán queda pegado al spot (±0.2%, misma tolerancia que `gexAnalysis`) el `advice` es `"lateral"`; si el imán señala una dirección pero NINGÚN strike del vecindario confirma con net premium real todavía, `advice` es `"esperar_breakout"` — ambos casos muestran el mensaje en la UI en vez de forzar una entrada. Ruta `app/api/magnet-wall/route.ts` (SPX fijo, JSON simple — no SSE, igual que `/api/0dte`, porque la cadena de tastytrade es rápida) + componente `ContratosVecinos2Tab.tsx` con un mapa visual del GEX 0DTE (barras por strike, verde/rojo call/put, imán 🧲 y zona de inversión ↕ marcados). Verificado en vivo (ago 2026): con SPX en $7728.20 e imán en $7725 (0.04% de distancia), correctamente mostró "NO OPERAR — MERCADO LATERAL" en vez de forzar una señal.
    - **Update ago-2026 — umbral de "lateral" pasó de % del spot a grilla real de strikes:** un imán a 0.2% del spot (≈15 pts en SPX) se descartaba como "lateral" — Carlos señaló que en opciones esos pocos puntos ya son plata real. Ahora `strikeStep()` (lib/magnetWall.ts) detecta el paso real entre strikes (mediana de los huecos, para no dejarse arrastrar por uno raro) y solo es "lateral" si el imán cae en el MISMO strike que el spot. Regresión cubierta con el caso real (SPX $7728.20, imán $7725) que antes salía mal.
    - **Update ago-2026 — probabilidades no se muestran con 0 horas al cierre:** `probTouch` con 0 horas restantes da 0% real (redondeado al piso de 3%, que parecía un dato pero era un artefacto matemático de "sin tiempo" — el 0DTE de índice ya venció). La UI (`ContratosVecinos2Tab.tsx`) oculta el veredicto/targets y muestra "🔒 MERCADO CERRADO" en su lugar cuando `hoursToClose === 0`; el mapa del GEX se mantiene visible igual.
    - **Update ago-2026 — selector de ticker igual a Agente ODTE, pero ES/NQ con DATOS REALES de futuro (no alias de SPX/NDX):** a diferencia de Agente ODTE (donde ES/NQ reusan la cadena de SPX/NDX), acá Carlos pidió poder operar mientras el mercado de acciones está cerrado — CME (ES/NQ) cotiza casi 24/5. `lib/tastytrade.ts` agregó `fetchActiveFuture` (`/instruments/futures?product-code[]=`, filtra `active-month: true` — el contrato más líquido/rolado), `fetchFuturesQuote` (`market-data/by-type?future=`) y `fetchNestedFuturesOptionChain` + `fetchFuturesOptionQuotesByType` (`/futures-option-chains/{producto}/nested`, símbolos propios de tastytrade con espacios tipo `"./ESU6 E2CQ6 260812C7750"`, NO formato OCC). `lib/futuresChain.ts` → `fetchActiveFuturesOptionChain` resuelve el futuro activo + la expiración diaria MÁS PRÓXIMA que todavía no dejó de cotizar (`stops-trading-at > now`, así sigue funcionando de noche cuando la de hoy ya cerró pero la de mañana ya está lista) y devuelve el MISMO shape `ZRow[]` que `fetchZeroDteChain` — permite reusar `zeroDteGex`/`atmIV` sin tocarlos. **MarketSnack NO cubre CME** (comprobado en vivo: pedir "ES" ahí devuelve la ACCIÓN Eversource Energy, no el futuro — por eso `/api/magnet-wall` nunca intenta net premium para ES/NQ). `magnetWallSignal` ahora acepta `hasFlowCoverage` (default `true`) para explicar la falta de confirmación con un mensaje distinto cuando NO hay fuente de flujo (vs. cuando sí la hay pero hoy no confirma nada) — el imán se sigue mostrando como "posible entrada", solo que marcado `⏳ SIN CONFIRMACIÓN`. Horario propio `isFuturesMarketOpen` (`lib/marketHours.ts`): abierto domingo 18:00 ET a viernes 17:00 ET, con pausa de mantenimiento diaria 17:00-18:00 ET. Verificado en vivo con el mercado de acciones cerrado: ES real en $7751.75 (imán $7700, PUT, 48% de probabilidad real) y NQ real en $29,663.75 (imán $29,600, PUT, 95%) — ambos con horas reales al cierre de la opción diaria (no 0), a diferencia de SPX/SPY/QQQ que si están fuera de 9:30-16:00 ET.
    - **Update ago-2026 — targets confirmados para ES/NQ vía POSICIONAMIENTO real (no flujo):** Carlos pidió explícitamente poder ver targets confirmados también de noche, no solo el imán suelto marcado "sin confirmar". `classifyPositioning` (lib/magnetWall.ts) clasifica cada strike del vecindario por Open Interest × gamma REAL de tastytrade (los mismos `ZeroDteGexNode.callGex`/`putGex` que ya calcula `zeroDteGex`) en vez de net premium: mucho OI/gamma en calls por encima del spot = resistencia estructural (sesgo bajista), mucho OI/gamma en puts por debajo = soporte estructural (sesgo alcista) — misma convención de dominio de siempre (venta de calls = resistencia, venta de puts = soporte), pero sin poder distinguir compra/venta real (eso solo lo da el flujo ejecutado, que no existe para CME). `MagnetWallInput.positioningLevels` es un respaldo: solo se usa en los strikes donde el flujo real (`strikePremiums`) no dio señal (`netBias === 0`) — el flujo real, cuando existe, siempre gana. `MagnetLevel` ahora trae `source: "flow" | "positioning"` para que el texto de cada target diga la verdad sobre qué lo confirmó (`reasonFor` bifurca en base a esto). La ruta arma `positioningLevels` directo de `gex.nodes` para ES/NQ. Verificado en vivo: NQ pasó de "⏳ ESPERAR — SIN CONFIRMACIÓN" a **"entrar"** con targets reales ($29675 72%, $29670 76%, imán $29600 95%) y ruptura real ($29690 46%, $29730 42%), todos con la gamma real en dólares.
    - **Bug real encontrado y resuelto en el camino — condición de carrera entre tickers:** al cambiar de ticker (o al restaurar el guardado de `localStorage` al montar), dos fetches en vuelo podían resolver fuera de orden — la respuesta VIEJA llegando después de la nueva pisaba los datos correctos con los de otro ticker (se vio en vivo: la UI decía "/NQ" pero mostraba datos de SPX). `ContratosVecinos2Tab.tsx` ahora guarda el ticker pedido en un `ref` y descarta cualquier respuesta que ya no coincida con el último pedido (`requestedTickerRef`), y además espera a que `localStorage` se restaure antes de disparar el primer fetch (`tickerReady`) para no gastar un fetch inicial del ticker por defecto que de todas formas se va a descartar.
    - **4 targets por lado (ago 2026, pedido explícito de Carlos):** `MAX_STEP_TARGETS = 3` (+ el imán = 4 "hacia el imán") y `MAX_BREAKOUT_TARGETS = 4` en `lib/magnetWall.ts` — antes eran 2 de cada lado.
    - **Update ago-2026 — dos mejoras para "menos probabilidad de trader perdedor" (pedido explícito de Carlos):**
      1. **Filtro de percentil contra ruido de Open Interest** (`POSITIONING_WALL_PERCENTILE = 0.7`, misma idea que ya usa `lib/levels.ts`): a diferencia del net premium (que es literalmente $0 cuando no hubo operaciones — un filtro con sentido), el Open Interest real casi nunca es exactamente cero, así que `classifyPositioning` sin piso dejaba que CUALQUIER strike vecino "confirmara" con solo tener algo de OI. Ahora solo cuenta como pared real el top 30% de gamma del propio vecindario (`percentile()`, calculado sobre `callGex`/`putGex` > 0 de todo el vecindario). Verificado en vivo: bajó de 3 a 2 targets confirmados hacia el imán y de 2 a 1 en ruptura para NQ — menos targets, pero los que quedan son paredes reales, no ruido.
      2. **Descuento por régimen de gamma** (`REGIME_DISCOUNT = 0.75`): la tesis "el precio gravita al imán" solo es fuerte con γ **positiva** (dealers anclan) — con γ **negativa** amplifican el movimiento en vez de revertirlo, mismo principio que ya usaba `closingForecast` (`lib/zerodte.ts`, confianza "baja" fuera de γ positiva) pero que `magnetWallSignal` no aplicaba. Ahora `regime` (de `gex.regime`, pasado desde la ruta para SPX/SPY/QQQ **y** ES/NQ) descuenta las probabilidades de los targets HACIA el imán (no los de ruptura — con γ negativa un movimiento en contra del imán es, si acaso, más plausible, no menos) y agrega un aviso explícito en `reason` + un chip `⚠ γ NEGATIVA` en la UI.
    - **Update ago-2026 — "Mapa del GEX" rediseñado como tabla option-chain** (pedido explícito de Carlos, con captura de referencia del estilo de tabla de Agente ODTE): reemplazó la lista de barras por una tabla real CALLS | STRIKE | PUTS (Volumen/OI/Delta a cada lado, fila "PRECIO ACTUAL" que separa arriba/abajo del spot, igual que `.z-chain` en `AgenteOdteTab.tsx`). La ruta (`app/api/magnet-wall/route.ts`) arma `chainLines: GexChainLine[]` (tipo compartido en `app/prueba-de-fuego/types.ts`) emparejando `rows`/`chain.rows` por strike vía `buildGexChainLines`; `pickDisplayStrikes` decide qué strikes mostrar: los ~22 más cercanos al spot **más** los que el propio `signal` ya marcó como target (aunque queden fuera de esa ventana, para no perder un soporte/resistencia real solo por estar un poco lejos). **Soportes y resistencias clave marcados visiblemente:** solo los strikes que aparecen en `towardTargets`/`breakoutTargets` (no cualquier strike con señal — eso sería volver a meter el ruido que el filtro de percentil recién sacó) llevan una fila resaltada + chip `RESISTENCIA`/`SOPORTE`/`COMPRA CALLS`/`COMPRA PUTS` según `MagnetLevel.callSignal`/`putSignal` de ese strike — así la tabla y el panel de targets de abajo siempre muestran EXACTAMENTE los mismos niveles, verificado en vivo (NQ: RESISTENCIA en $29750/$29700/$29670/$29650, los mismos 4 strikes de los targets).
    - **Update ago-2026 — confirmación cruzada SPX/NDX para ES/NQ cuando el mercado de acciones está abierto** (pedido explícito de Carlos: "cuando abre el mercado combina MarketSnack y tastytrade para futuros, para mayor precisión"): fuera de esa ventana, ES/NQ solo tienen posicionamiento (Open Interest × gamma, ver arriba) porque MarketSnack no cubre CME — pero SÍ cubre el índice hermano (SPX para ES, NDX para NQ). `fetchCrossMarketFlow` (`app/api/magnet-wall/route.ts`) corre, solo con `isMarketOpen(now)` en `true`, la MISMA señal de pared que ya usa "SPX vecinos" (`wallEntrySignal`, `lib/neighborContracts.ts`) sobre la cadena REAL del índice hermano — no intenta mapear strikes de ES a strikes de SPX (la base ES-SPX varía y mapear mal strikes sería peor que no mapear nada), solo compara la DIRECCIÓN. `magnetWallSignal` (`lib/magnetWall.ts`) recibe eso como `crossMarketFlow: { direction, underlying }`: si coincide con el imán y no había ninguna otra confirmación, sube `advice` de `"esperar_breakout"` a `"entrar"` con un boost moderado (`CROSS_MARKET_BOOST = 1.15`, menor que una pared confirmada en el propio instrumento — nunca se apilan, la pared propia manda); si CONTRADICE, descuenta (`CROSS_MARKET_CONFLICT_DISCOUNT = 0.6`) y avisa explícito en `reason`. Envuelto en `.catch(() => null)` en la ruta — es una mejora, no debe tumbar la respuesta de ES/NQ si MarketSnack falla. Extraje `fetchNetPremiumNeighborhood` (compartida por el camino de índice y esta confirmación cruzada) para no duplicar la lógica de armar el vecindario + net premium.
  - **"Contratos vecinos 3.0" (ago 2026, pedido explícito de Carlos, con ejemplo práctico dado a mano — SPX en 7750):** a diferencia de "Contratos vecinos 2.0" (el imán del GEX decide la dirección, el dinero real solo confirma), acá NO hay GEX — el dinero real de MarketSnack decide todo. Solo SPX/SPY/QQQ (mismo universo que Agente ODTE), sin ES/NQ. `lib/contratosVecinos3.ts` (PURO, tests en `contratosVecinos3.test.ts`): mira 10 strikes de **CALLS arriba** del spot y 10 de **PUTS abajo** (nunca al revés), y en dos pasos: (1) el **Premium Traded** (`ask_premium+bid_premium+mid_premium`, TODA la actividad de hoy sin importar dirección — a diferencia de `summarizeTradeBuckets` que descarta el mid a propósito, acá sí cuenta) marca dónde "está el dinero" — un strike solo es candidato a target si su Premium Traded es ≥50% del máximo de su propio lado (`HIGH_ACTIVITY_RATIO`); (2) el **Net Premium** ahí confirma o descarta: positivo (compra agresiva al ask) = target confirmado tanto en calls (alcista) como en puts (bajista, misma convención de `lib/magnetWall.ts`); negativo (venta agresiva) = **pared real** (resistencia en calls, soporte en puts) que frena el camino ahí mismo, sin más targets detrás. Da hasta 2 targets por lado; si el siguiente strike con actividad tiene MENOS Premium Traded que el último target confirmado, ese target queda marcado como `capStrike` (probable techo del movimiento — pedido explícito: "si el premium traded es menor... puede que sea su punto máximo"). El lado ganador (mayor actividad real en su primer strike relevante, sea target o pared) fija la dirección; el lado CONTRARIO aporta un nivel de **stop-loss**: el primer strike de actividad baja (≤15% del máximo de TODO el vecindario, no solo ese lado — `LOW_ACTIVITY_RATIO`) con net premium negativo ahí. Ruta `app/api/contratos-vecinos-3/route.ts` (JSON simple, no SSE): spot en vivo combina MarketSnack (`fetchAssetPrice`, fuente principal) y tastytrade (`fetchZeroDteChain`, respaldo + ya trae la cadena para resolver símbolos OCC). `lib/marketsnack.ts` → `fetchContractActivitySummaries` (nueva, junto a `fetchContractPremiumSummaries`) resume con `summarizeActivity` en vez de `summarizeTradeBuckets` para no perder el mid ni la serie temporal que hace falta para la tendencia. Componente `ContratosVecinos3Tab.tsx`, refresco cada 30s (más agresivo que los 60s de "2.0" — pedido explícito: "siempre tiene que actualizarse") — cada strike muestra su tendencia reciente (`subiendo`/`bajando`/`estable`, comparando la mitad más nueva de los buckets del día contra la más vieja). Verificado en vivo (2026-08-13, mercado cerrado): spot $7798.99 coincidió al centavo con el cierre real de SPX: net premium negativo en TODOS los strikes del vecindario (normal hacia el cierre) → correctamente no confirmó ningún target, solo marcó la pared en $7800 y el stop-loss en $7765 — "ante la duda, no operar".
  - **Backtest real del día completo (2026-08-13) + 3 mejoras (pedido explícito de Carlos, con preguntas puntuales sobre momentos reales del día):** se reconstruyó la sesión completa con los buckets reales de MarketSnack (mismo endpoint, `trade_summaries?period=1d`) y precio real de SPX (Robinhood), replay minuto a minuto de `contratosVecinos3Signal`. Encontró 3 problemas reales, los 3 corregidos en `lib/contratosVecinos3.ts` (tests nuevos que reproducen cada caso):
    1. **Filtro de persistencia** (`applyPersistence`, `PERSISTENCE_REQUIRED = 3`): dos señales del día real duraban 1-2 minutos antes de invertirse (parpadeos, no confirmaciones) — inoperables aunque la regla las diera por válidas. Ahora una dirección solo se marca `confirmed` si `type` + presencia de `target1` se sostienen 3 lecturas seguidas; el STRIKE de `target1` puede subir/bajar mientras tanto sin resetear la cuenta ("dejar correr" una posición real no debe penalizarse). PURA — el historial de lecturas crudas lo acumula el caller (`ContratosVecinos3Tab.tsx`, en un `ref`, reiniciado al cambiar de ticker), no vive en el servidor.
    2. **Stop-loss acotado** (`STOP_LOSS_MAX_STRIKES = 4`, antes buscaba en los 10 strikes completos): un caso real (12:15 ET) dio un stop-loss a 49 puntos de la entrada — "correcto" según la regla, inservible como gestión de riesgo real. Ahora solo se sugiere un stop-loss si hay un strike de baja actividad + net premium negativo dentro de los 4 strikes más cercanos al spot; más allá de eso, mejor `null` que un número inútil.
    3. **Bug real de selección de "ganador"** (encontrado al responder por qué la herramienta no daba PUT en el pico real de las 10:35 ET, spot $7816): antes, el lado ganador se elegía por `leadPremium` crudo (dólares acumulados en el primer strike de actividad alta, fuera target o PARED) — eso dejaba que una pared de venta (sin ninguna señal de entrada real) le ganara a un target de compra confirmado del otro lado solo por tener más dólares acumulados en el día. Ese día real, a las 10:35 ET el call solo tenía una pared en $7820 mientras el put YA tenía dos targets confirmados en $7810/$7805 — la herramienta seguía diciendo CALL justo en el techo. `pickWinner()` ahora prioriza cualquier lado con al menos un target CONFIRMADO sobre un lado con solo pared, y entre dos lados confirmados, gana el de mayor Premium Traded en su primer target — el criterio de magnitud cruda solo se usa si NINGÚN lado tiene targets confirmados. **Cambió el resultado en producción**: los mismos datos reales de cierre del 13-ago que antes daban "CALL sin target, solo pared en $7800" ahora dan correctamente "PUT confirmado en $7795".
    4. **`supportingWall` (pedido explícito, con ejemplo real):** a las 09:36 ET (spot $7783.57, señal CALL en $7785) Carlos preguntó si también se podía confirmar que $7780/$7775 se estaban VENDIENDO en puts — sí: `below.wallStrike` ($7780, net premium negativo) ya se calculaba pero se descartaba. Ahora se expone como `signal.supportingWall` (strike + `"resistencia"`/`"soporte"` + net premium) — la pared REAL del lado contrario (venta agresiva, no solo silencio) que refuerza la tesis del lado ganador. Se menciona en `reason` y se muestra como chip en la UI.
- **Backtest manual sobre datos reales (viernes 31-jul-2026, SPX, cada 5 min, ventana de 5 días — 0DTE no tiene datos históricos, ver limitación de MarketSnack más abajo):** motor de pared vs. walk de siempre sobre el MISMO día: **+$4,586 (18/27 aciertos, 67%)** contra **+$2,656 (19/33, 58%)**. La comparación contra el código GEX puro (pre-30-jul, `gex.kingStrike` como target sin caminar vecinos) fue peor todavía: de 9 señales que dio ese día, **las 9 apuntaban a strikes con volumen $0** (hasta 1,100 puntos del spot) — no una señal desactualizada, contratos directamente inoperables.
  - **Limitación real de MarketSnack para backtesting (no es bug propio):** `filter[date][gte/lte]` de `flow_feed` solo re-fecha el timestamp de los trades — los contratos que aparecen siempre salen de la cadena **EN VIVO de hoy**, nunca de una reconstrucción histórica real. Un contrato ya vencido (como un 0DTE de un día pasado) nunca puede aparecer, sin importar qué fecha se pida. Verificado tres veces (piso de premium bajo, dos días distintos, comparación contra consulta en vivo sin filtro — mismos vencimientos siempre). Consecuencia: **ningún backtest de SPX 0DTE de un día pasado es posible** con este endpoint; para reconstruir un día completo de SPX hace falta ADEMÁS subir el piso de premium mucho (**$150,000** para el 31-jul — $30K y $75K seguían truncando la paginación y perdiendo la mañana) porque MarketSnack pagina hacia atrás en el tiempo. Y ese piso tan alto rompe la medición de precio (la mayoría de contratos solo tienen 1 trade grande en el día) — el precio real de entrada/salida hay que sacarlo de `trade_summaries` (sin piso de premium, trae TODO trade de ese contrato) derivando precio promedio = `(ask_premium+bid_premium+mid_premium) / (volumen×100)` del bucket de 5 min más cercano, no de `flow_feed` directo.
- **Selector de idioma ES/EN (ago 2026, pedido explícito de Carlos: "que el proyecto sea factible para personas que hablen inglés o español"):** `lib/i18n.tsx` (`LocaleProvider`/`useLocale`, diccionario por namespaces + interpolación `{var}`, componente `Rich` para textos con `<b>`/`<i>` embebido vía `dangerouslySetInnerHTML` — contenido propio, nunca de usuario) + `app/components/LanguageSwitcher.tsx` (dropdown nativo en el header, siempre visible). Persiste en `localStorage` (`visionary.locale`) con detección inicial por idioma del navegador; el server siempre renderiza `"es"` (coincide con `<html lang="es">`) y recién en el cliente se resuelve el real, para no romper la hidratación. **Cobertura (ago 2026):** todo el chrome global (`NavTabs`, `HeaderBar`, `HeroLanding`), la vista **Estudiante** completa y la vista **Pro** completa — las ~20 tarjetas del panel avanzado (`SentimentCard`, `PredictionCard`, `ActivityCard`, `MoneyFlowCard`, `LevelsCard`, `NewsCard`, `ProWallsCard`, `GexHeatmapCard`, `TradesFeed`, `CompanyHeader`, `ScorecardPanel` + las 6 categorías del scorecard, `AggressionScoreCard`, `ConvictionCard`, `ConvictionTransactions`, `UnusualityCard`, `StructureCard`, `IvContextCard`, `ValidationCard`, `FlowPriceChart`, `OptionChainTable`, `ChartPanel`, `RepeatBadge`) — títulos, labels, headers de tabla y veredictos por umbral (los que son texto estático condicionado por score, no generado). El header (buscador de ticker + botones rápidos) solo se muestra una vez que se eligió un ticker — antes duplicaba al buscador de la landing. **Lo que queda en español a propósito:** la prosa generada dinámicamente por las funciones puras del motor de análisis (`prediction.summary`, `prediction.caveat`, `Scenario.driver` en `lib/prediction.ts`; `Level.why` en `lib/levels.ts`; `s.note`/`s.iv.band`/`s.rank.band` en `lib/ivcontext.ts`; `s.verdict`/`s.speed.band` en `lib/validation.ts`; `EXECUTION_LABEL` en `lib/flow.ts`; títulos/reasoning de noticias, que vienen de la fuente) — traducirla requiere tocar esas funciones puras (con sus tests), no solo los componentes; y el resto de páginas (`/ideas`, `/wheel`, `/venta-de-primas`, `/prueba-de-fuego`, `/unusual-swing`, `/flow`, `/ajustes`).
- Detalle completo en [web/SPEC.md](web/SPEC.md).

- **Agente Principal (de Opciones):** primer agente y núcleo del sistema. Su especificación completa está en [Proceso Principal](Agente%20Principal/Proceso%20Principal.md).
- **Sub Agentes:** aún no definidos. La Tarea 4 (Buy Put) menciona "validación de contexto con **otros agentes**", así que el diseño contempla sub-agentes de confirmación (p. ej. contexto macro, técnico o de noticias).

## Responsabilidades del Agente Principal (resumen)

1. **Open Interest** por fecha de vencimiento, ordenado de mayor a menor.
2. **Volumen más alto** por expiración + almacenar **≥5 días** de histórico para detectar patrones recurrentes.
3. **Comparación sectorial:** identificar las 5 líderes del sector y determinar si el flujo es **sectorial o individual** (con etiqueta).
4. **Interpretación de Call/Put** (ver tabla abajo).
5. **Segmentación con fórmulas** (Open Premium, Notional Value).
6. **Evaluación de liquidez** del Option Chain con alertas.
7. **Monitoreo RSS** de noticias.

Detalle completo en [Proceso Principal](Agente%20Principal/Proceso%20Principal.md).

## Reglas de dominio (críticas)

### Interpretación de flujo
| Operación | Señal |
|-----------|-------|
| Buy Call | Direccional (alcista) |
| Sell Call | Resistencia / posible "muro" en órdenes grandes |
| Buy Put | Hedge **o** direccional → **requiere validación de contexto con otros agentes** |
| Sell Put | Soporte del subyacente |

### Fórmulas
```
Open Premium   = Open Interest × Precio del Contrato (Bid)
Notional Value = Open Interest × 100 × Strike        # zonas de relevancia si expira ITM
```

### Liquidez — **regla de seguridad prioritaria**
- Comparar el nocional promedio de 5 días de las **"7 Magníficas"** contra la cadena consultada.
- **Alertar "datos no fiables"** si: disparidad de liquidez **20–40%** vs. líderes, **o** liquidez **< 60%** del promedio.
- **Nunca recomendar operar una opción ilíquida.** Si la cadena es ilíquida, marcarla explícitamente; esto aplica también a la interpretación del **GEX**. Ver [Instrucciones y Referencias](Intrucciones%20Referencias.md).

### Noticias
Monitorear los feeds definidos en [RSS Feed](RSS%20Feed.md) (CNBC + Investing.com) y adjuntar noticias relevantes al panel de resultados.

## Convenciones para trabajar aquí

- **Idioma:** la documentación y los prompts del agente están en **español**. Mantener ese idioma salvo indicación contraria.
- **Fuente de verdad:** los `.md` son la versión editable; los `.pages`/`.pdf` son los originales de referencia. Al actualizar reglas, editar el `.md` correspondiente y reflejar el cambio aquí.
- **Al implementar código:** este directorio es documentación de diseño. Cualquier implementación (parser de option chain, motor de flujo, lector RSS) debe respetar las fórmulas y umbrales exactos de arriba.
- La regla de **liquidez/GEX** es una salvaguarda: ante la duda, no operar y avisar.

## Rebrand a Visionary Trades (ago 2026)

El proyecto se llama **Visionary Trades** en todo el texto visible, la documentación, el
código, `localStorage` (con migración automática de las keys viejas `tito.*`, ver
`web/lib/legacyStorage.ts`) y en disco: la carpeta pasó de `agente-tito-metralleta` a
`visionary-trades`, y el dev server de `.claude/launch.json` (nivel Desktop) de `tito-web`
a `visionary-web`. Los specs/plans históricos de `docs/superpowers/` (fechados 24-jul-2026)
se dejaron intactos a propósito — son actas de una sesión de planificación pasada.
