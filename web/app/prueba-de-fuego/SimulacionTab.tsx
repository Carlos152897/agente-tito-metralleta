"use client";

// Simulación — reemplaza a "Registro de Operaciones" en este mismo botón.
// Contenido pendiente de definir (Carlos, 2026-08-04).

export default function SimulacionTab() {
  return (
    <div className="empty-note">
      Simulación — todavía sin definir qué va acá.
    </div>
  );
}
